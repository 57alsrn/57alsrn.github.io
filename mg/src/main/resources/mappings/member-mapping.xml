<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member-mapping">

<select id="IDCK"  parameterType="MemberVo"  resultType="MemberVo">
   select  *  from  member 
   where id=#{id}      
</select>


<select id="LOGIN"  parameterType="MemberVo"  resultType="MemberVo">
   select  *  from  member 
   where id=#{id}  and pwd=#{pwd}    
</select>

	
<select id="SELECT"  parameterType="MemberVo" resultType="MemberVo">
   select  *  from  member 
   where 1 = 1
   <if test="ch1=='name'" >
      <![CDATA[
        and  name  like '%'||#{ch2}||'%'
      ]]>
    
   </if>
   <if test="ch1=='roadAddress'" >
       <![CDATA[
         and  roadAddress  like '%'||#{ch2}||'%'
       ]]>
   </if>
</select>

<insert id="INSERT">
insert  into member(id, pwd, name, tel, postcode, roadAddress , detailAddress, extraAddress   ) 
  values(#{id}, #{pwd}, #{name}, #{tel}, #{postcode}, #{roadAddress} , #{detailAddress}, #{extraAddress}  )
</insert>

<delete id="DELETE"  parameterType="ProductVo" >
  delete  from  member 
  where id=#{id}
</delete>
    
	
	
</mapper>
